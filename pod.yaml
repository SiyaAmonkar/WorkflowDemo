apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: hello-world-
  annotations:
    argocd.argoproj.io/hook: Sync
spec:
  entrypoint: whalesay
  templates:
  - name: whalesay
    script:
      image: ubuntu:18.04
      command: [bash]
      source: |
        argo list
